import React from 'react';
import Image from 'next/image';
import { motion } from 'framer-motion';

type Props = {};

const About = (props: Props) => {
  return (
    <section
      id="about"
      className="pt-[60px] w-full h-screen flex"
    >
      About
    </section>
  );
};

export default About;
